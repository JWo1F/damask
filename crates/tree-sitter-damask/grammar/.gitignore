# Keep the generated parser (src/) committed — Zed compiles src/parser.c and
# does not run `tree-sitter generate`, and so does this crate's build.rs.
# Regenerate with `tree-sitter generate --abi 14` after editing grammar.js.
/bindings/
/build/
/node_modules/
Makefile
binding.gyp
*.wasm
